import React from 'react';

const footerStyle = {
    background: "black",
    fontWeight: "bold"
};

function Footer() {
    return (
        <div style={footerStyle} className="footer">
            Footer
        </div>
    );
}